export default function Footer() {
  return (
    <footer className="border-t border-[var(--line)] mt-16">
      <div className="mx-auto max-w-6xl px-6 py-8 text-sm text-[var(--ink-soft)] flex flex-wrap justify-between gap-4">
        <p>Common Cause — every order carries a receipt of where the money goes.</p>
        <p>Built as a portfolio project.</p>
      </div>
    </footer>
  );
}
